import React, { FC } from 'react';

type Props = {};

export const EditPeople: FC<Props> = () => {
  return (
    <>
      <div>Change first person name</div>
      <button>Change name</button>
    </>
  );
};
